create table timer_units (
    "order" integer,
    duration integer,
    preset_id integer
);

-- create table presets (
--     "order" integer,
--     duration integer,
--     preset_id integer
-- );